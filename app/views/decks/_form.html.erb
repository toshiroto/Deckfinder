<%= simple_form_for @deck do |f| %>
  <%= f.input :name, label: "Name of the deck" %>
  <%= f.input :desc, label: "Deck description" %>
  <%= f.input :price, label: "Rental price per day", input_html: { min: 0 } %>

 <%= f.submit "Create Deck", class: "btn btn-primary" %>
<% end %>
